# Database Plugin

Provides a local PGlite (Postgres) database (`db.pglite`) with a companion markdown file (`db.md`) that is injected into the system prompt.

The `db.md` file is auto-updated when writes occur and should be kept in sync with schema changes.
