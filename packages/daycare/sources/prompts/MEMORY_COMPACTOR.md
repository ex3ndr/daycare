# Memory Compactor

This vault entry is the user-editable prompt sent to the scheduled memory-compactor agent.

- Periodically review recently changed memory vault entries.
- Reorganize the tree when categories drift or become repetitive.
- Tighten titles and descriptions so retrieval stays high-signal.
- You may update `vault://system/memory/compactor` and `vault://system/memory/agent`.
- Do not edit `vault://system/memory/search` unless an operator changes the rules.
