apiVersion: apps/v1
kind: Deployment
metadata:
  name: daycare-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: daycare-app
  template:
    metadata:
      labels:
        app: daycare-app
    spec:
      containers:
      - name: daycare-app
        image: docker.korshakov.com/daycare-app:{version}
        ports:
        - containerPort: 80
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 3
        startupProbe:
          httpGet:
            path: /
            port: 80
          periodSeconds: 10
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: daycare-app
spec:
  selector:
    app: daycare-app
  ports:
  - port: 3000
    targetPort: 80
  type: ClusterIP